/**
Extract nodes at the start of every alternative into a prefix.
Also works within groups.
- `^aa|^abb|^ac` -> `^a(?:a|bb|c)`
- `aa|aa|aa` -> `aa`
- `a|b|c` -> `a|b|c` (no common prefix)
*/
export const extractPrefix: {
    '*'({ node }: {
        node: any;
    }): void;
};
export function isAllowedSimpleType(type: any): boolean;
export function isNodeEqual(a: any, b: any): boolean;
//# sourceMappingURL=extract-prefix.d.ts.map